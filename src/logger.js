// handle actual on-disk logging, rotation, etc. use morgan?
var fs = require('fs'),
    winston = require('winston');

(function(Logger) {

  Logger.init = function(app) {
    // todo: figure this out
  };

}(exports))
